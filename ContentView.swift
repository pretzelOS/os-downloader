import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("PretzelOS Downloader").font(.largeTitle).fontWeight(.bold).frame(maxWidth: .infinity).padding()
             .frame(alignment: .top)
            Button("Download") {
              
            }
            Text("-------------------")
            
                
        }
    }
}
